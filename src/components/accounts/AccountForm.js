import {Container, Form, Button }from "react-bootstrap";
import { Formik } from "formik";
import "../../styles/components/accounts/account.scss";
const AccountForm = () => {
    return (
        <Container>

        </Container>
    );
};

export default AccountForm;